doctype xml
urlset(xmlns='https://www.sitemaps.org/schemas/sitemap/0.9')
    each content in published_content
        url
            loc #{config.site_host}#{content.uri}
            lastmod #{formatter.format(content.date, "yyyy-MM-dd")}