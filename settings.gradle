rootProject.name = 'duck'
include ':agent:util'
include ':agent:sensor'
include ':agent:service'

include ':sample:standalone-app'
include ':sample:tapestry5-war'

include 'server/agent-api'
rootProject.children.find { it.name == 'server/agent-api' }.name = 'agent-api'

