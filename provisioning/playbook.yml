---
#---------------------------------------------------------------------------
# Configures all servers once the infrastructure has been provisioned.
# See also infrastructure.yml
#---------------------------------------------------------------------------
- {include: infrastructure.yml, tags: [aws, infrastructure]}
- {include: os-maintenance.yml, tags: [server, os, patches]}
- {include: database.yml, tags: [server, database]}
- {include: application.yml, tags: [server, application]}
