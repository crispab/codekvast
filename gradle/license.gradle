apply plugin: 'license'

license {
    header file("$rootDir/LICENSE")
    includes (['**/main/**.java', '**/main/**.sql'])
}

licenseFormat {
    description "Adds the correct license header to all source files"
    group "Licensing"
}

downloadLicenses {
    group "Licensing"
}
