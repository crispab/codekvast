description = "Support utilities that can be used anywhere in Codekvast except in the collector"

apply from: "$rootDir/gradle/java-8.gradle"
apply from: "$rootDir/gradle/sources-javadoc.gradle"
apply from: "$rootDir/gradle/license.gradle"

archivesBaseName = "codekvast-commons"

dependencies {
    compile lombok

    testCompile junit
    testCompile mockito
}
