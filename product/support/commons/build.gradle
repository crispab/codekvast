description = "Support utilities that can be used anywhere in Codekvast"

apply from: "$rootDir/gradle/java-6.gradle"

archivesBaseName = "codekvast-support-commons"

dependencies {
    compile lombok

    testCompile junit
    testCompile mockito
}
