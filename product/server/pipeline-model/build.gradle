apply from: "$rootDir/gradle/java-11.gradle"
apply from: "$rootDir/gradle/license.gradle"

description = "Model objects shared between producers and consumers"
archivesBaseName = "codekvast-pipeline-model"
version = codekvastVersion

dependencies {
    implementation 'javax.validation:validation-api:2.0.1.Final'

    testImplementation testFrameworks
    testImplementation "com.google.code.gson:gson:$gsonVersion"
}
