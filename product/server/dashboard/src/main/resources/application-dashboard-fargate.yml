# This profile is activated when deploying to AWS ECS Fargate
codekvast:
  dashboard:
    fileImportQueuePath: /codekvast/queue
    agent-statistics.delay.seconds: 31536000 # one year
    fileImportInitialDelaySeconds: 31536000 # one year

spring:
  profiles:
    include: common-fargate